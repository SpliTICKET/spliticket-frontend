export type userType = {
	username: string;
	password?: string;
	email?: string;
	firstName?: string;
	lastName?: string;
};

export type animalType = {
	animalId: string;
	name: string;
	species: string;
};
